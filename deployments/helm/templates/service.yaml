apiVersion: v1
kind: Service
metadata:
  name: {{ include "eden-ops.fullname" . }}
  labels:
    {{- include "eden-ops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eden-ops.selectorLabels" . | nindent 4 }}