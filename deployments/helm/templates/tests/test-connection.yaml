apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eden-ops.fullname" . }}-test-connection"
  labels:
    {{- include "eden-ops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eden-ops.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never